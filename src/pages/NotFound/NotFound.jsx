import { Container, Title } from './NotFound.styled';

const NotFound = () => {
  return (
    <Container>
      <Title>Sorry, but no page found</Title>
    </Container>
  );
};

export default NotFound;
